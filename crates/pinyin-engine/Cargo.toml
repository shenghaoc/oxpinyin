[package]
name = "pinyin-engine"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "Supported pinyin-rs session API — the stable Rust surface."

# Supported Rust surface; SemVer freezes with the first public release.
[lints]
workspace = true

[dependencies]
