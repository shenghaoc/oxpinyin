[package]
name = "pinyin-lambda"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
publish = false
description = "Training-only deleted-interpolation λ estimator reproducing libpinyin gen_deleted_ngram + estimate_interpolation. Never ships."

[lints]
workspace = true

[[bin]]
name = "pinyin-lambda"
path = "src/bin/pinyin-lambda.rs"

[dependencies]
pinyin-segment = { path = "../pinyin-segment" }
pinyin-counter = { path = "../pinyin-counter" }
