---
inclusion: always
---
# Product

Rust re-expression of libpinyin (IBus first). Stage 1: oracle parity. Stage 2:
measured upgrades. Claims: re-expression is fine; never replace / succeed /
outperform. Scoped measurements only. See `ROADMAP.md` and `AGENTS.md`.
