[package]
name = "pinyin-user"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "oxpinyin internal crate — no stability promise; use pinyin-engine."

[lints]
workspace = true

[dependencies]
pinyin-core = { workspace = true }
redb = { workspace = true }
