[package]
name = "oxpinyin-chewing"
version.workspace = true
edition.workspace = true
license.workspace = true
repository.workspace = true
rust-version.workspace = true
description = "The shared chewing-key surface: the packed key, its display renderers, and the frozen content tables."

[lints]
workspace = true

[dev-dependencies]
# keyboard-tables property test; pinned to the same version as the
# core/user/engine property suites (workspace lock pins =1.11.0).
proptest = { version = "=1.11.0", default-features = false, features = ["std"] }
