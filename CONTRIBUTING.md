# Contributing

Pre-alpha scaffold. Run the README Quickstart (fmt / clippy / test).

```sh
git config core.hooksPath .githooks   # once per clone
```

AI assistants must follow `AGENTS.md` (`Assisted-by: AGENT:MODEL` only; set
semantics).
